Alces-Sacct

Run
```
bin/alces-sacct.rb report
```


Sub flags
```
--csv -c return a csv file
--verbose -v return all values being read
-- Allows adding of sacct sub commands as an arbitrary argument after it is written
